{
  "name": "stroop-pwa",
  "version": "5.6.0",
  "module": "server.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run server.ts",
    "prepare:cap": "node scripts/prepare-capacitor.cjs",
    "build": "npm run prepare:cap",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "bun-types": "latest"
  }
}
